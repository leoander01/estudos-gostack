import React from 'react';
import { render } from 'react-dom';

import App from './App';

// JSX = HTML dentro do JS (JS XML)

// Renderizar ('@babel/preset-env',)
render(<App />, document.getElementById('app'))